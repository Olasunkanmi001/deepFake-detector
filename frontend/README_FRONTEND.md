# Frontend (Next.js) - Deepfake Detector (MVP)

## Quick start
1. Install dependencies:
   npm install

2. Run dev server:
   npm run dev

3. Open http://localhost:3000

## Notes
- This simplified UI posts uploaded files to the backend at http://localhost:8000/analyze
- In production, proxy requests or set environment variable to point to your backend URL.
